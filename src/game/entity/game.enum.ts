export enum Type {
  PUBLIC = "PUBLIC",
  PRIVATE = "PRIVATE",
}

export enum Mode {
  NORMAL = "NORMAL",
  LADDER = "LADDER",
}

export enum Status {
  READY = "READY",
  INGAME = "INGAME",
  DONE = "DONE",
}

export enum Role {
  WINNER = "WINNER",
  LOSER = "LOSER",
}
